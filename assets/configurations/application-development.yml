
logging.config: ${boku.cloud.config.uri}/${spring.application.name}/development/master/logback.xml

metadata:
  config: ${boku.cloud.config.uri}/${spring.application.name}/development/master/metadata.json
